server:
  port: 8093

spring:
  application:
    name: ms-sucursal

  datasource:
    url: jdbc:mysql://localhost:3306/db_cine_sala_test?createDatabaseIfNotExist=true
    username: root
    password: root
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false 
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.MySQLDialect