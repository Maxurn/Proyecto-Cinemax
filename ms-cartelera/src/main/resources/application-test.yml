server:
  port: 0

spring:
  application:
    name: ms-cartelera

  datasource:
    url: jdbc:mysql://localhost:3306/db_cartelera_test?createDatabaseIfNotExist=true
    username: root
    password: root
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false 
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.MySQLDialect